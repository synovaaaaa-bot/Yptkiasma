// Globals Index - Easy import from one place
export { header, default as headerGlobal } from './header';
export { footer, default as footerGlobal } from './footer';
export { siteSettings, default as siteSettingsGlobal } from './site-settings';

// Re-export types
export type { Header, Footer, SiteSettings, NavLink, SocialLink } from '../types/globals';
